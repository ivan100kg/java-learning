// Создание объектов. Понятие «конструктор». Объявление и вызов методов

Methods
    public static void main(String[] args) {// code;}
    public  // access modifier
    static  // non-access modifier(s)
    void    // retun type
    main    // name
    ()      // params
    {}      // body

Constructor
    Car car1 = new Car();   // Car() - constructor, as name of class
    Car(){}                 // default constructor
    Car(vars){code;}        // custom constructor
    // конструктор ничего не возвращает
    int salary(p1,p2){      // метод класса с 2-мя парам, возвращ int
        return this.salary;
    }
